import { visited } from "./visited.js";

export function knightMoves() {
  return {
    findShortest(startPoint, endPoint) {
      console.table(`Start point: ${startPoint}`);
      console.log(`End point: ${endPoint}`);
    },
  };
}
