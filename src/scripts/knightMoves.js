import { ids } from "webpack";
import { visited } from "./visited.js";

export function knightMoves() {
  return {
    findShortest(startPoint, endPoint) {
      /*       console.table(`Start point: ${startPoint}`);
      console.log(`End point: ${endPoint}`);
      function move(startPoint, endPoint) {
        if (startPoint === endPoint) {
          console.log("Found shortest path!");
        }
        /!we have at most 8 avaible moves:
        console.log(`Move left: ${startPoint[0] - 1}`);

        //down-left
        if (startPoint[0] - 1 < 0) {
          console.log(`nope`);
          move(startPoint, endPoint);
        }
      } */
    },
  };
}
